import {
    createSlice,
    nanoid,
    createAsyncThunk,
    current,
    createSelector,
} from "@reduxjs/toolkit";


const initialState = {
    
};


const teacherSlice = createSlice({
    name: "teacher",
    initialState: initialState,
    reducers: {
     
    },
  
});



